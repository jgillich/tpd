version: 1
meta:
  description: Trivy vulnerability scanner
extends:
  - defaults
  - mise
command: ["trivy"]
caches:
  trivy: ~/.cache/trivy
mounts:
  ~/.config/trivy:
    source: ~/.config/trivy
    create: true
    read_only: false
tools:
  trivy: latest
