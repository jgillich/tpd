version: 1
meta:
  description: SSH keys and client
mounts:
  ~/.ssh:
    source: ~/.ssh
    optional: true
  ~/.ssh/known_hosts:
    source: ~/.ssh/known_hosts
    read_only: false
    optional: true
packages:
  - openssh-client
